#include <stdio.h>
#include <stdlib.h>

int main()
{
    int b,h;
    int area;
    printf("enter the base:");
    scanf("%d",&b);
    printf("enter the highst:");
    scanf("%d",&h);
    area=.5*b*h;
    printf("the area of triangle is %d",area);
    return 0;
}
